name: Terraform Plan and Comment

on:
  pull_request:
  push:
    branches-ignore:
      - main

jobs:
  terraform-update-pr:
    environment:
      bucketname = 'f29baf0e-81df-4090-a5a5-f33cb9566b0d'
    
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v3

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_wrapper: false

    - name: Terraform fmt
      id: fmt
      run: terraform fmt -check -diff
      continue-on-error: true

    - name: Terraform Init
      id: init
      run: terraform init
      env:
        GOOGLE_CREDENTIALS: ${{ secrets.TERRAFORM_SA }}

    - name: Terraform Validate
      id: validate
      run: | 
        echo stdout=$(terraform validate -no-color)| tee -a "$GITHUB_OUTPUT"
      env:
        GOOGLE_CREDENTIALS: ${{ secrets.TERRAFORM_SA }}

    - name: Terraform Plan
      id: plan
      if: github.event_name == 'pull_request'
      run: |
        terraform plan -out=plan.out
        terraform show -json plan.out > plan.json
        output=$(terraform show plan.out -no-color)
        #output="${output//'%'/'%25'}"
        #output="${output//$'\n'/'%0A'}"
        #output="${output//$'\r'/'%0D'}"
        echo stdout=${output} >> "$GITHUB_OUTPUT"
        echo mytest="test1\ntest2" >> "$GITHUB_OUTPUT"
        
      continue-on-error: true
      env:
        GOOGLE_CREDENTIALS: ${{ secrets.TERRAFORM_SA }}

    - name: list files
      if: github.event_name == 'pull_request'
      run: ls -la

    # Does not have access to terraform state or service account
    - name: diff
      if: github.event_name == 'pull_request'
      run: docker run --rm -v "$(pwd):/src" hahomdal/rover -standalone -planJSONPath=plan.json

    - name: unzip rover generated file
      if: github.event_name == 'pull_request'
      run: unzip rover.zip -d ${{ github.run_id }}

    - name: Generate diff svg
      if: github.event_name == 'pull_request'
      run: docker run --rm -v "$(pwd):/src" hahomdal/rover  -genImage=true -planJSONPath=plan.json
      
    - name: convert svg to png
      if: github.event_name == 'pull_request'
      run: |
        sudo apt-get install gsfonts
        convert -background none -density 1000 -resize 1000x rover.svg ${{ github.run_id }}/rover.png
       
    # Maintained by google
    - id: 'google-auth'
      if: github.event_name == 'pull_request'
      uses: 'google-github-actions/auth@v1'
      with:
        credentials_json: '${{ secrets.TERRAFORM_SA }}'
   
    # Maintained by Seth Vargo, googler
    - id: 'google-upload-file'
      if: github.event_name == 'pull_request'
      uses: 'google-github-actions/upload-cloud-storage@v1'
      with:
        path: ${{ github.run_id }}
        destination: 'f29baf0e-81df-4090-a5a5-f33cb9566b0d'
        process_gcloudignore: false
        
    - uses: actions/github-script@v6
      if: github.event_name == 'pull_request'
      env:
        PLAN: "${{ steps.plan.outputs.stdout }}"
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          //const exec = require('@actions/exec');
          //const core = require('@actions/core');

          let myOutput = '';
          let myError = '';
          const options = {};
          options.listeners = {
            stdout: (data) => {
              myOutput += data.toString();
            },
            stderr: (data) => {
              myError += data.toString();
            }
          };
          //options.cwd = '/src';
          //await exec.exec('/bin/base64', ['-i', 'rover.svg'], options);

          

          core.info('Review URL: https://storage.googleapis.com/f29baf0e-81df-4090-a5a5-f33cb9566b0d/${{ github.run_id }}/index.html');
          // 1. Retrieve existing bot comments for the PR
          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
          })
          const botComment = comments.find(comment => {
            return comment.user.type === 'Bot' && comment.body.includes('Terraform Format and Style')
          })

          // 2. Prepare format of the comment
          const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
          #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
          #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
         
          <details><summary>Validation Output</summary>
          
          \`\`\`\n
          ${{ steps.validate.outputs.stdout }}
          \`\`\`\n

          </details>

          #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`

          <details><summary>Show Plan</summary>
          
          \`\`\`\n
          ${{ process.env.PLAN }}
          \`\`\`\n
          </details>

          <details><summary>Show Plan</summary>
          
          \`\`\`\n
          ${{ steps.plan.outputs.mytest }}
          \`\`\`\n
          </details>

          *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`, Working Directory: \`${{ env.tf_actions_working_dir }}\`, Workflow: \`${{ github.workflow }}\`*
          [![Visual Diff](https://storage.googleapis.com/f29baf0e-81df-4090-a5a5-f33cb9566b0d/${{ github.run_id }}/rover.png)](https://storage.googleapis.com/f29baf0e-81df-4090-a5a5-f33cb9566b0d/${{ github.run_id }}/index.html)`;

          // 3. If we have a comment, update it, otherwise create a new one
          if (botComment) {
            github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: botComment.id,
              body: output
            })
          } else {
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })
          }

    #- name: Upload failed tests
    #  if: github.event_name == 'pull_request'
    #  uses: edunad/actions-image@v2.0.0
    #  with:
    #    path: './*.svg'
    #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #    title: 'Failed E2E tests 🙀'
        # annotationLevel: 'failure'
      #      run: |
      #  PLAN=$(terraform plan)
      #  echo "$PLAN" > plan.txt

      #    - name: Add Plan as PR Comment
      #if: github.event_name == 'pull_request'
      #run: |
      #  PR_ID=$(gh pr view https://github.com/${{ github.repository }}/pull/${{ github.event.pull_request.number }} --json number -q ".number")
      #  gh pr comment $PR_ID --body "Terraform Plan Output:\n\`\`\`\n$(cat plan.txt)\n\`\`\`"
